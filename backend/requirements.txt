fastapi==0.141.1
# Pinned directly: FastAPI's floor (>=0.46.0) still admits versions with known
# advisories; 1.3.1 is the first release clear of all of them.
starlette==1.6.0
uvicorn[standard]==0.30.0
sqlalchemy==2.0.35
alembic==1.13.0
asyncpg>=0.30.0
pydantic==2.9.0
pydantic-settings==2.5.0
# python-jose was dropped: its 3.3.0 CVEs are fixed in 3.4.0, but PYSEC-2025-185
# has no fix at all, and it hard-depends on `ecdsa`, whose Minerva timing
# advisory (PYSEC-2026-1325) the maintainers have declined to fix. PyJWT covers
# the HS256 signing this codebase actually uses.
pyjwt==2.13.0
cryptography==50.0.1
passlib[bcrypt]==1.7.4
celery[redis]==5.4.0
redis==5.1.0
python-json-logger==2.0.7
# Observability. docs/deploy.md and the compose stack already point a
# scraper at /metrics, which nothing served; unhandled exceptions went to
# the log and nowhere else. Both versions checked against OSV before
# pinning — sentry-sdk has four advisories, none affecting 2.69.1.
prometheus-client==0.26.0
sentry-sdk==2.69.1
httpx==0.27.0
python-multipart==0.0.31
boto3==1.35.0
stripe==10.0.0
anthropic==0.34.0
openai==1.50.0
langchain>=0.3.7
# The async client is what app/ actually constructs — health.py and
# knowledge_vault/search.py both build AsyncElasticsearch — and its default
# transport needs aiohttp, which elasticsearch does not pull in. Without it
# every knowledge-vault request dies on
# "You must have 'aiohttp' installed to use AiohttpHttpNode".
elasticsearch==8.15.0
aiohttp==3.14.3
sendgrid==6.11.0
pillow==12.3.0
python-barcode==0.15.1
python-docx==1.1.0
ebooklib==0.18
# weasyprint==62.0  # Commented out: requires GTK on Windows, optional (PDF export)
flower==2.0.0
pytest==9.1.1
pytest-asyncio==1.4.0
# All three were imported but never declared; local virtualenvs happened to
# have them, so CI was the only place it showed. pytest-cov backs the
# --cov flags the test job passes, EmailStr needs email-validator, and the
# test fixtures run on SQLite.
pytest-cov==7.0.0
# A per-test timeout: without one, a single hanging test burns the job's
# full six-hour budget and reports nothing at all about where it stopped.
pytest-timeout==2.4.0
email-validator==2.3.0
aiosqlite==0.22.1
defusedxml==0.7.1
mypy==1.11.2
ruff==0.6.0
locust==2.31.0

# Transitive pins — floors raised only to clear known advisories. Each of these
# is pulled in by something above; remove a pin once its parent's own floor
# passes it.
langsmith>=0.8.18     # via langchain
msgpack>=1.2.1        # via celery / cachecontrol
tornado>=6.5.8        # via flower / locust

# VoiceForge Integration
pydub>=0.25.1
pyloudnorm>=0.1.1
soundfile>=0.12.1
# Pinned, not floored: mypy type-checks against whatever numpy stubs are
# installed, so an open floor lets a numpy release turn the lint job red
# overnight with no code change — which is exactly what happened.
numpy==2.5.3
scipy==1.18.1
websockets>=12.0
mutagen>=1.47.0
ffmpeg-python>=0.2.0
# faster-whisper>=1.0.0  # Commented out: requires CUDA/ctranslate2, optional (VoiceForge ASR)
deepgram-sdk>=3.0.0
