"use client";

import { CompetitorDashboard } from "@/modules/competitors/components/CompetitorDashboard";

export default function CompetitorsPage() {
  // Note: This page only renders the CompetitorDashboard component.
  // Translation is handled within the component itself.
  return <CompetitorDashboard />;
}
