"use client";

import { PricingDashboard } from "@/modules/pricing/components/PricingDashboard";

export default function PricingPage() {
  // Note: This page only renders the PricingDashboard component.
  // Translation is handled within the component itself.
  return <PricingDashboard />;
}
